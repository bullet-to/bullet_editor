/// Test-side entry point for the gauntlet fixture document
/// (v3-build-strategy.md §walking skeleton). The canonical builder lives in
/// the package so the dev inspector can load the same document.
library;

export 'package:bullet_editor/src/dev/gauntlet_document.dart';
